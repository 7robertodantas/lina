# Backend WebSocket Configuration
# The Go backend handles all MQTT communication and state management
NEXT_PUBLIC_BACKEND_WS_URL=ws://localhost:8080/ws

# Legacy MQTT Configuration (now handled by backend)
# These variables are no longer needed by the UI
# NEXT_PUBLIC_MQTT_BROKER_URL=wss://your-mqtt-broker-url.com
# NEXT_PUBLIC_MQTT_USERNAME=your-username
# NEXT_PUBLIC_MQTT_PASSWORD=your-password
# NEXT_PUBLIC_DEFAULT_DEVICE_ID=smart-meter-001
