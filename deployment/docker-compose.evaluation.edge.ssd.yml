services:
  redis:
    volumes:
      - ${EDGE_DATA_ROOT}/redis:/data

  mosquitto:
    volumes:
      - ${EDGE_DATA_ROOT}/mosquitto:/mosquitto/data
      - ../infrastructure/certs:/mosquitto/certs:ro

  device:
    volumes:
      - ../infrastructure/certs/ca.crt:/certs/ca.crt:ro
      - ../infrastructure/certs/edge.crt:/certs/edge.crt:ro
      - ../infrastructure/certs/edge.key:/certs/edge.key:ro
      - ${EDGE_DATA_ROOT}/device:/data

  ledger:
    volumes:
      - ../infrastructure/certs/ca.crt:/certs/ca.crt:ro
      - ../infrastructure/certs/server.crt:/certs/server.crt:ro
      - ../infrastructure/certs/server.key:/certs/server.key:ro
      - ${EDGE_DATA_ROOT}/ledger:/data

  consumption:
    volumes:
      - ${EDGE_DATA_ROOT}/consumption:/data

  lightning:
    volumes:
      - ../infrastructure/certs/ca.crt:/certs/ca.crt:ro
      - ../infrastructure/certs/server.crt:/certs/server.crt:ro
      - ../infrastructure/certs/server.key:/certs/server.key:ro
      - ${EDGE_DATA_ROOT}/lightning:/data
