# Edit ansible_host / ansible_user for your edge host (e.g. Multipass ubuntu@<ip>).
# Binaries: by default Ansible cross-compiles on this Mac (see inventory/group_vars lina_build_*).
# To copy pre-built binaries instead: set lina_build_binaries=false and lina_binaries_dir.

[edge]
edge-node ansible_host=192.168.2.2 ansible_user=ubuntu
